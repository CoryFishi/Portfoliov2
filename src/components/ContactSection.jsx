export default function ContactSection({ contactRef }) {
  return (
    <div
      ref={contactRef}
      className="w-full h-full flex items-center justify-center text-center bg-blue-50"
    >
      Contact
    </div>
  );
}
